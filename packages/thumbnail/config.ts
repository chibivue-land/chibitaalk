// import { fileURLToPath } from "node:url";
import { defineSatoriConfig } from "x-satori/vue";

// const __dirname = n/ew URL(fileURLToPath(import.meta.url));

export default defineSatoriConfig({
  height: 628,
  width: 1200,
  fonts: [],
  props: {},
});
